apiVersion: notification.toolkit.fluxcd.io/v1beta2
kind: Alert
metadata:
  name: epp-deploy
  namespace: flux-system
spec:
  summary: "EPP deploy ${app_env}"
  providerRef:
    name: epp-github-dispatch
  eventSeverity: info
  eventSources:
    - kind: Kustomization
      name: '*'
      namespace: 'epp--previews'
