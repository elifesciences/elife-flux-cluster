---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: descheduler
  namespace: kube-system

spec:
  interval: 1m
  releaseName: kube-system-descheduler
  chart:
    spec:
      chart:  descheduler
      version: 0.26.0
      sourceRef:
        kind: HelmRepository
        name: descheduler
      interval: 1m
