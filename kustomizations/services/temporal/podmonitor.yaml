---
apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: temporal
  namespace: temporal
spec:
  selector:
    matchExpressions:
    - key: app
      operator: In
      values:
        - temporal
  podMetricsEndpoints:
  - targetPort: 9233
