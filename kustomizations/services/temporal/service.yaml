apiVersion: v1
kind: Service
metadata:
  name: temporal
  namespace: temporal
  labels:
    app_name: epp
    app_env: ${app_env}
    app_tier: import
spec:
  selector:
    app: temporal
  ports:
  - name: grpc
    port: 7223
  - name: ui
    port: 8223
