apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: data-hub
  namespace: data-hub
spec:
  values:
    airflow:
      image:
        repository: docker.io/elifesciences/data-hub-with-dags # {"$imagepolicy": "data-hub--prod:data-hub-stable:name"}
        tag: 0.0.95 # {"$imagepolicy": "data-hub--prod:data-hub-stable:tag"}
