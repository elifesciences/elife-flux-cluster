apiVersion: image.toolkit.fluxcd.io/v1beta1
kind: ImagePolicy
metadata:
  name: epp-node-prototype
  namespace: epp
spec:
  imageRepositoryRef:
    name: epp-node-prototype
    namespace: epp
  filterTags:
    pattern: '^master-[a-fA-F0-9]+-(?P<ts>.*)'
    extract: '$ts'
  policy:
    numerical:
      order: asc
