apiVersion: notification.toolkit.fluxcd.io/v1beta2
kind: Alert
metadata:
  name: epp-deploy
  namespace: epp
spec:
  summary: "EPP deploy ${app_env}"
  providerRef:
    name: github-dispatch
  eventSeverity: info
  eventSources:
    - kind: Kustomization
      name: 'epp--preview'
