apiVersion: image.toolkit.fluxcd.io/v1beta1
kind: ImagePolicy
metadata:
  name: basex-validator-stable
  namespace: basex-validator
spec:
  imageRepositoryRef:
    name: basex-validator-stable
    namespace: basex-validator
  policy:
    semver:
      range: '*'
