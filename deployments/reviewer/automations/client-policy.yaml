apiVersion: image.toolkit.fluxcd.io/v1beta1
kind: ImagePolicy
metadata:
  name: libero-reviewer-client
  namespace: reviewer
spec:
  imageRepositoryRef:
    name: libero-reviewer-client
    namespace: reviewer
  filterTags:
    pattern: '^master-[a-fA-F0-9]+-(?P<ts>.*)'
    extract: '$ts'
  policy:
    numerical:
      order: asc
