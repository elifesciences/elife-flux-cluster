apiVersion: image.toolkit.fluxcd.io/v1beta1
kind: ImagePolicy
metadata:
  name: sciencebeam
  namespace: reviewer
spec:
  imageRepositoryRef:
    name: sciencebeam
    namespace: reviewer
  policy:
    semver:
      range: '*'
