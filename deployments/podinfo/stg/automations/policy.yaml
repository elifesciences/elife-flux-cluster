apiVersion: image.toolkit.fluxcd.io/v1beta1
kind: ImagePolicy
metadata:
  name: podinfo--stg
  namespace: podinfo
spec:
  imageRepositoryRef:
    name: podinfo
    namespace: podinfo
  policy:
    semver:
      range: '>=6.0.0'
