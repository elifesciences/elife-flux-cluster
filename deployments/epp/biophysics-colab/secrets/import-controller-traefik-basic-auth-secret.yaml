apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: epp-import-controller-traefik-http-basic-auth
  namespace: epp--biophysics-colab
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: secret-store
    kind: ClusterSecretStore
  target:
    name: epp-import-controller-traefik-http-basic-auth
    creationPolicy: Owner
    template:
      engineVersion: v2
      data:
        usersFile: "{{ .auth }}"
  dataFrom:
  - extract:
      key: epp-team/biophysics/import-controller-http-basic-auth
